# =====================================================
# MONGODB ATLAS CONFIGURATION
# Configurazione per MongoDB Atlas per audit data
# =====================================================

# MongoDB Atlas Connection String (richiesto)
# Formato: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
# Ottieni da: https://cloud.mongodb.com/
MONGODB_CONNECTION_STRING=mongodb+srv://isru-admin:admin@isru-league.zxnbzud.mongodb.net/?retryWrites=true&w=majority&appName=isru-league

# Database Name (opzionale, default: isru_metrics)
MONGODB_DATABASE_NAME=isru_metrics

# Connection Pool Settings (opzionali)
MONGODB_MAX_POOL_SIZE=10
MONGODB_MIN_POOL_SIZE=2
MONGODB_MAX_IDLE_TIME_MS=30000

# Timeout Settings (opzionali)
MONGODB_SERVER_SELECTION_TIMEOUT_MS=5000
MONGODB_SOCKET_TIMEOUT_MS=45000
MONGODB_CONNECT_TIMEOUT_MS=10000

# Retry Settings (opzionali)
MONGODB_RETRY_WRITES=true
MONGODB_RETRY_READS=true

# =====================================================
# SETUP INSTRUCTIONS
# =====================================================

# 1. Crea un cluster MongoDB Atlas:
#    - Vai su https://cloud.mongodb.com/
#    - Crea un nuovo progetto
#    - Crea un cluster (M0 Free Tier è sufficiente per iniziare)
#    - Configura Network Access (aggiungi il tuo IP o 0.0.0.0/0 per tutti)
#    - Crea un Database User con read/write permissions

# 2. Ottieni la connection string:
#    - Nel dashboard MongoDB Atlas, clicca "Connect"
#    - Scegli "Connect your application"
#    - Copia la connection string e sostituisci <password> con la tua password

# 3. Per sviluppo locale:
#    - Copia questo file in .env.local
#    - Sostituisci i valori con quelli del tuo cluster

# 4. Per produzione (Vercel):
#    - Vai su Vercel Dashboard > Settings > Environment Variables
#    - Aggiungi MONGODB_CONNECTION_STRING con il valore del tuo cluster
#    - Opzionalmente aggiungi le altre variabili per personalizzare la configurazione

# =====================================================
# SECURITY NOTES
# =====================================================

# - NON committare mai questo file con credenziali reali
# - Usa sempre password forti per i database users
# - Limita l'accesso di rete quando possibile
# - Abilita MongoDB Atlas IP Whitelist
# - Considera l'uso di MongoDB Atlas Private Endpoints per produzione

# =====================================================
# MIGRATION SETTINGS
# =====================================================

# Per lo script di migrazione da Vercel Blob
DRY_RUN=true
VERBOSE=true

# Vercel Blob Token (necessario solo per la migrazione)
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token
